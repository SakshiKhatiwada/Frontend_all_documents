let Name = [];
 
Name=["Sakshi", "Yojana", "Bibek", "Manish", "Aayush", "Birendra"];

let length = Name.length;
console.log("Length of Array is: " + length);

for (let y of Name)
    {
        console.log(y);
    }

    console.log(`Second last element is: ${Name[Name.length - 2]}`);